export const metadata = {
    title: "Verify Email | ChatHub",
    description: "Verify your email address.",
};

export default function VerifyEmailLayout({
    children, // will be a page or nested layout
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
